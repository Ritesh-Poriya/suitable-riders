export enum VersionOsType {
  ANDROID = 'android',
  IOS = 'ios',
}

export enum AppType {
  MERCHANT = 'merchant',
  RIDER = 'rider',
}

export enum UpdateStatusType {
  SOFT_UPDATE = 'SOFT_UPDATE',
  HARD_UPDATE = 'HARD_UPDATE',
}
